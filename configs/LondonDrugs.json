{
  "client": "LondonDrugs",
  "ciCd": { "github-actions": true },
  "webhooks": { "teams": { "url": "${TEAMS_WEBHOOK_URL}" } },
  "emailReporting": {
    "enabled": false,
    "smtp": { "host": "${SMTP_HOST}", "port": 587, "user": "${SMTP_USER}", "pass": "${SMTP_PASS}" }
  },
  "testManagement": {}
}
